# API URL (Vite dev server proxies /api to this by default)
VITE_API_URL=

# Clerk (production auth — leave blank for local JWT dev)
# Get key from https://dashboard.clerk.com > API Keys
VITE_CLERK_PUBLISHABLE_KEY=
